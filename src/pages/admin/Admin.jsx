import Login from "@/components/login/Login";
import React, { memo } from "react";

const Admin = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default memo(Admin);
